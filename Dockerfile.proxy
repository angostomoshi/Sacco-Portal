FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install --production

# Copy the proxy server file
COPY proxy-server.js ./

# Expose the port the proxy server runs on
EXPOSE 3023

# Start the proxy server
CMD ["node", "proxy-server.js"]
